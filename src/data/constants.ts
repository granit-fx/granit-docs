/** Single source of truth for documentation-wide constants. */

/**
 * Packable projects under `src/` across granit-dotnet (358), granit-business (148),
 * granit-iot (24) and granit-website (30).
 */
export const PACKAGE_COUNT = 560;

/** Public packages under `packages/@granit` in granit-front (91 SDK + 80 React + 64 React UI). */
export const FRONTEND_PACKAGE_COUNT = 235;
export const CULTURE_COUNT = 18;
export const BASE_LANGUAGE_COUNT = 15;
export const REGIONAL_VARIANT_COUNT = 3;
export const PATTERN_COUNT = 60;
export const ADR_COUNT = 74;
