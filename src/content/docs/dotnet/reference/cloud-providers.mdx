---
title: "Cloud Providers \u2014 AWS, Azure & Sovereign Cloud"
description: All Granit packages organized by cloud provider — AWS, Azure, and Google Cloud
sidebar:
  label: Cloud Providers
  order: 35
---

import { Tabs, TabItem } from "@astrojs/starlight/components";

Granit is cloud-agnostic by design. Each module defines provider-neutral abstractions
(`IIdentityProvider`, `IBlobStorageProvider`, `IEncryptionService`, etc.) and ships
dedicated provider packages for major cloud platforms. Switch providers by changing
a single NuGet reference and DI registration — no application code changes needed.

:::note
This page covers the three major cloud providers (AWS, Azure, Google Cloud).
Other providers (Keycloak, HashiCorp Vault, Brevo, etc.) are documented in
their respective module pages — see [Provider Compatibility](/dotnet/reference/provider-compatibility/)
for the full matrix.
:::

## At a glance

| Domain | [AWS](#aws) | [Azure](#azure) | [Google Cloud](#google-cloud) | [Alibaba Cloud](#alibaba-cloud) |
| ------ | :---------: | :-------------: | :---------------------------: | :-----------------------------: |
| Authentication | ✓ | ✓ | ✓ | planned |
| Identity | ✓ | ✓ | ✓ | planned |
| Vault / Encryption | ✓ | ✓ | ✓ | planned |
| Blob Storage | ✓ | ✓ | ✓ | planned |
| Notifications — Email | ✓ | ✓ | <span style="color: var(--sl-color-red);">✗</span> | planned |
| Notifications — SMS | ✓ | ✓ | <span style="color: var(--sl-color-red);">✗</span> | planned |
| Notifications — Mobile Push | ✓ | ✓ | ✓ | planned |
| **Total packages** | **7** | **7** | **5** | **0** |

## Setup

<Tabs>
<TabItem label="AWS">

```csharp
// Program.cs — AWS provider stack
builder.AddGranit(granit => granit
    .AddModule<GranitAuthenticationJwtBearerCognitoModule>());

builder.AddGranitBlobStorageS3();

builder.Services.AddGranitIdentityCognito();
builder.Services.AddGranitVaultAws();
builder.Services.AddGranitNotificationsAwsSes();
builder.Services.AddGranitNotificationsAwsSnsSms();
builder.Services.AddGranitNotificationsAwsSnsMobilePush();
```

</TabItem>
<TabItem label="Azure">

```csharp
// Program.cs — Azure provider stack
builder.AddGranit(granit => granit
    .AddModule<GranitAuthenticationJwtBearerEntraIdModule>());

builder.AddGranitBlobStorageAzureBlob();

builder.Services.AddGranitIdentityEntraId();
builder.Services.AddGranitVaultAzure();
builder.Services.AddGranitNotificationsAcsEmail();
builder.Services.AddGranitNotificationsAcsSms();
builder.Services.AddGranitNotificationsAzureNotificationHubs();
```

</TabItem>
<TabItem label="Google Cloud">

```csharp
// Program.cs — Google Cloud provider stack
builder.AddGranit(granit => granit
    .AddModule<GranitAuthenticationJwtBearerGoogleCloudModule>());

builder.AddGranitBlobStorageGoogleCloud();

builder.Services.AddGranitIdentityGoogleCloud();
builder.Services.AddGranitVaultGoogleCloud();
builder.Services.AddGranitNotificationsGoogleFcm();
```

</TabItem>
</Tabs>

---

## AWS

Amazon Web Services packages use the official AWS SDK for .NET.

| Package | Module | What it does |
| ------- | ------ | ------------ |
| `Granit.Authentication.JwtBearer.Cognito` | Authentication | JWT validation + claims transformation for Cognito User Pools |
| `Granit.Identity.Federated.Cognito` | Identity | `IIdentityProvider` via Cognito User Pool Admin API |
| `Granit.Vault.Aws` | Vault | `IEncryptionService` via AWS KMS + dynamic credentials via Secrets Manager |
| `Granit.BlobStorage.S3` | Blob Storage | `IBlobStorageProvider` via S3-compatible API (also works with MinIO, Scaleway, etc.) |
| `Granit.Notifications.AwsSes` | Notifications | `IEmailSender` via Amazon Simple Email Service |
| `Granit.Notifications.AwsSns` | Notifications | `ISmsSender` + `IMobilePushSender` via Amazon SNS (capabilities activated per config section) |

**See also:**
[Identity](/dotnet/security/identity/) —
[Vault & Encryption](/dotnet/data/vault/) —
[Blob Storage](/dotnet/data/blob-storage/) —
[Notifications](/dotnet/infrastructure/notifications/)

---

## Azure

Microsoft Azure packages use `DefaultAzureCredential` for authentication
(Managed Identity, Azure CLI, etc.).

| Package | Module | What it does |
| ------- | ------ | ------------ |
| `Granit.Authentication.JwtBearer.EntraId` | Authentication | JWT validation + claims transformation for Microsoft Entra ID (Azure AD) |
| `Granit.Identity.Federated.EntraId` | Identity | `IIdentityProvider` via Microsoft Graph API |
| `Granit.Vault.Azure` | Vault | `IEncryptionService` via Azure Key Vault |
| `Granit.BlobStorage.AzureBlob` | Blob Storage | `IBlobStorageProvider` via Azure Blob Storage |
| `Granit.Notifications.AzureCommunicationServices` | Notifications | `IEmailSender` + `ISmsSender` via Azure Communication Services (capabilities activated per config section) |
| `Granit.Notifications.AzureNotificationHubs` | Notifications | `IMobilePushSender` via Azure Notification Hubs |

:::note
AWS and Azure both cover all three notification channels (email, SMS,
and mobile push) through dedicated packages.
:::

**See also:**
[Identity](/dotnet/security/identity/) —
[Vault & Encryption](/dotnet/data/vault/) —
[Blob Storage](/dotnet/data/blob-storage/) —
[Notifications](/dotnet/infrastructure/notifications/)

---

## Google Cloud

Google Cloud packages use Application Default Credentials (ADC) or service account
JSON keys.

| Package | Module | What it does |
| ------- | ------ | ------------ |
| `Granit.Authentication.JwtBearer.GoogleCloud` | Authentication | JWT validation + claims transformation for Google Cloud Identity Platform (Firebase Auth) |
| `Granit.Identity.Federated.GoogleCloud` | Identity | `IIdentityProvider` via Firebase Admin SDK |
| `Granit.Vault.GoogleCloud` | Vault | `IEncryptionService` via Cloud KMS + dynamic credentials via Secret Manager |
| `Granit.BlobStorage.GoogleCloud` | Blob Storage | `IBlobStorageProvider` via Google Cloud Storage |
| `Granit.Notifications.GoogleFcm` | Notifications | `IMobilePushSender` via Firebase Cloud Messaging |

**See also:**
[Identity](/dotnet/security/identity/) —
[Vault & Encryption](/dotnet/data/vault/) —
[Blob Storage](/dotnet/data/blob-storage/) —
[Notifications](/dotnet/infrastructure/notifications/)

---

## Alibaba Cloud

:::caution[Planned]
Alibaba Cloud provider packages are planned but not yet implemented. Alibaba Cloud
offers mature equivalents for all domains: RAM/IDaaS (identity), KMS (encryption),
OSS (S3-compatible storage), DirectMail (email), SMS Service, and Push Notifications.

Contributions welcome — see [CONTRIBUTING.md](https://github.com/granit-fx/granit-dotnet/blob/main/CONTRIBUTING.md).
:::

| Domain | Alibaba Cloud service | Status |
| ------ | -------------------- | ------ |
| Authentication | IDaaS | planned |
| Identity | RAM + IDaaS | planned |
| Vault / Encryption | KMS | planned |
| Blob Storage | OSS (S3-compatible) | planned |
| Notifications — Email | DirectMail | planned |
| Notifications — SMS | Short Message Service | planned |
| Notifications — Mobile Push | Push Notifications | planned |

---

## Cloud-agnostic providers

In addition to cloud-specific packages, Granit offers providers that work with any
cloud platform:

| Package | Channel(s) | Service |
| ------- | ---------- | ------- |
| `Granit.Notifications.Brevo` | Email + SMS + WhatsApp | Brevo (formerly Sendinblue) |
| `Granit.Notifications.Scaleway` | Email | Scaleway TEM (sovereign EU) |
| `Granit.Notifications.SendGrid` | Email | SendGrid (Twilio) |
| `Granit.Notifications.Twilio` | SMS + WhatsApp | Twilio Messaging |
| `Granit.Notifications.Smtp` | Email | Any SMTP server |
| `Granit.Notifications.WebPush` | Web Push | VAPID (standard) |
| `Granit.Notifications.SignalR` | Real-time | SignalR (WebSocket) |

---

## Feature comparison

### Identity and Authentication

| Capability | AWS (Cognito) | Azure (Entra ID) | Google Cloud | Alibaba Cloud |
| ---------- | :-----------: | :---------------: | :----------: | :-----------: |
| JWT validation | ✓ | ✓ | ✓ | planned |
| Claims transformation | ✓ | ✓ | ✓ | planned |
| User CRUD | ✓ | ✓ | ✓ | planned |
| Role management | ✓ | ✓ | ✓ (custom claims) | planned |
| User cache sync | ✓ | ✓ | ✓ | planned |
| Health check | ✓ | ✓ | ✓ | planned |

### Vault and Encryption

| Capability | AWS KMS | Azure Key Vault | Google Cloud KMS | Alibaba Cloud KMS |
| ---------- | :-----: | :-------------: | :--------------: | :---------------: |
| Encrypt / Decrypt | ✓ | ✓ | ✓ | planned |
| Dynamic DB credentials | ✓ | <span style="color: var(--sl-color-red);">✗</span> | ✓ | planned |
| Secret storage | ✓ | ✓ | ✓ | planned |
| Key rotation | ✓ | ✓ | ✓ | planned |
| Tenant-isolated keys | ✓ | ✓ | ✓ | planned |
| Health check | ✓ | ✓ | ✓ | planned |

### Blob Storage

| Capability | AWS S3 | Azure Blob | Google Cloud Storage | Alibaba Cloud OSS |
| ---------- | :----: | :--------: | :------------------: | :---------------: |
| Presigned upload | ✓ | ✓ | ✓ | planned |
| Presigned download | ✓ | ✓ | ✓ | planned |
| Tenant isolation | ✓ | ✓ | ✓ | planned |
| Health check | ✓ | ✓ | ✓ | planned |

### Notifications

| Channel | AWS | Azure | Google Cloud | Alibaba Cloud |
| ------- | :-: | :---: | :----------: | :-----------: |
| Email | ✓ (SES) | ✓ (ACS) | <span style="color: var(--sl-color-red);">✗</span> | planned |
| SMS | ✓ (SNS) | ✓ (ACS) | <span style="color: var(--sl-color-red);">✗</span> | planned |
| Mobile Push | ✓ (SNS) | ✓ (ANH) | ✓ (FCM) | planned |

:::tip
Google Cloud covers mobile push via Firebase Cloud Messaging
(`Granit.Notifications.GoogleFcm`). For email and SMS, use
the cloud-agnostic providers: `Granit.Notifications.SendGrid` for email
and `Granit.Notifications.Twilio` for SMS — see [Cloud-agnostic providers](#cloud-agnostic-providers).
:::

## See also

- [Provider Compatibility Matrix](/dotnet/reference/provider-compatibility/) — which providers are tested vs. supported
- [Vault](/dotnet/data/vault/) — provider-specific secret/credential modules (HashiCorp, Azure, AWS, GCP)
- [Blob storage](/dotnet/data/blob-storage/) — cloud-specific storage providers
- [Notifications: Email channel](/dotnet/infrastructure/notifications/email-channel/) — SendGrid, Scaleway, Azure, AWS SES
- [Notifications: SMS & push](/dotnet/infrastructure/notifications/sms-push-realtime/) — Twilio, Azure Communication, AWS SNS
